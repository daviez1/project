export const statusColors: { pending: string, preparing: string, ready: string, completed: string } = {
    pending: 'bg-yellow-100 text-yellow-800',
    preparing: 'bg-blue-100 text-blue-800',
    ready: 'bg-green-100 text-green-800',
    completed: 'bg-gray-100 text-gray-800'
  };
export const statusColorsPlus: { pending: string, preparing: string, ready: string, completed: string } = {
    pending: 'bg-blue-100 text-blue-800',
    preparing: 'bg-green-100 text-green-800',
    ready: 'bg-gray-100 text-gray-800',
    completed: 'bg-yellow-100 text-gray-800',
  };